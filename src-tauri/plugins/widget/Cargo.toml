[package]
name = "tauri-plugin-widget"
version = "0.1.0"
description = "Android home screen widget (AppWidgetProvider + RemoteViews) bridge for OpenCalendar."
edition = "2021"
authors = ["open-calendar-mobile"]
license = "MIT"
links = "tauri-plugin-widget"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
tauri = { version = "2" }
